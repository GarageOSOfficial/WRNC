# Versioning Strategy

## Approach

WRNC uses a simple, predictable versioning strategy aligned to release discipline.

- Use semantic versioning for externally visible releases.
- Increment the patch version for small fixes and maintenance updates.
- Increment the minor version for feature or workflow additions that remain backward compatible.
- Increment the major version when breaking changes are introduced.
